<p id="notice"><%= notice %></p>

<h1>Listing Themes</h1>

<table>
  <thead>
  <% @themes.each do |theme| %>
    <div class="theme row clearfix">
      <h2>
        <%= link_to theme.name, theme %><br>
      </h2>

      </div>
    </div>
  <% end %>